package org.matsim.project;

/*import org.matsim.api.core.v01.Id;
import org.matsim.api.core.v01.events.LinkEnterEvent;
import org.matsim.api.core.v01.events.PersonDepartureEvent;
import org.matsim.api.core.v01.events.handler.LinkEnterEventHandler;
import org.matsim.api.core.v01.events.handler.PersonArrivalEventHandler;
import org.matsim.api.core.v01.events.handler.PersonDepartureEventHandler;
import org.matsim.api.core.v01.population.Person;

import java.util.HashMap;
import java.util.Map;
*/
public class SimpleLinkEventHandler /*implements LinkEnterEventHandler*/ {

}
    /*
    static Map<Id<Person>, Boolean> link6EnterEvents = new HashMap<>();

    @Override
    public void handleEvent(LinkEnterEvent event) {
        if (event.getLinkId().equals(6)) {
            link6EnterEvents.put(event.getVehicleId(), event.getTime());
        }
        System.out.println("LinkEnterEvent event; time " + event.getTime() + " -- linkId: " + event.getLinkId()
                + " VehiculeId: " + event.getVehicleId()) + "Nb of entry on link(6)" + link6EnterEvents.size();
    }
}*/
